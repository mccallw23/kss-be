{
  "name": "kss-stripe",
  "version": "1.0.0",
  "description": "Stripe backend for Kitchen Sink Sender",
  "private": true,
  "scripts": {
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "stripe": "^14.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
